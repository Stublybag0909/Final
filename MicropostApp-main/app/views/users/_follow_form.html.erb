<%# unless session[:user_id]?(@user.id) %>
<div id="follow_form">
  <% if @user.following?(User.first) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
</div>
<%# end %> 
